### os

常用方法:
```
os.arch()   //操作系统架构,比如x64
os.cpus()   //每个cpu信息,数组
os.freemem() //系统空闲内存的字节数
os.totalmem() //所有内存,字节数返回
os.hostname() //系统主机名, root@hostname:$
os.platform() // aix,darwin,freebsd,linux,openbsd,sunos,win32等
os.release() //操作系统发行版本 比如4.2.0-36-generic
```